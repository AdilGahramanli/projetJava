CREATE TABLE Personnage(
                           id INT PRIMARY KEY,
                           nom VARCHAR(20),
                           points_de_vie INT,
                           type VARCHAR(20)
);